# VITE_WS_URL=ws://localhost:8787
# Uncomment and set the above variable ONLY if you need an explicit WebSocket override.
# By default, the application will dynamically connect to port 8787 on the host device.
